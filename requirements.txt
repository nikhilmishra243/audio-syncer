sounddevice>=0.4.6
Flask>=2.3.0
websockets>=11.0
numpy>=1.24.0

# Platform-specific notes:
#
# Linux: May need PortAudio development files
#   Ubuntu/Debian: sudo apt-get install portaudio19-dev python3-pyaudio
#   Fedora/RHEL: sudo dnf install portaudio-devel
#   Arch: sudo pacman -S portaudio
#
# macOS: PortAudio usually works out of the box
#   If issues: brew install portaudio
#
# Windows: Works out of the box with pip install
psutil